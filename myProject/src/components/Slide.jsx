import React from "react";

const Slide = ({ content }) => {
  return (
    <div className="flex justify-center items-center bg-white text-xl h-full">
      {content}
    </div>
  );
};

export default Slide;
